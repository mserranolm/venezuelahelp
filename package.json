{
  "name": "venezuelahelp",
  "private": true,
  "workspaces": [
    "backend",
    "infra",
    "frontend-public",
    "frontend-admin"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  }
}
